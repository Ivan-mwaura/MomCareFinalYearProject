const MomLoginModel = require('../Models/MomLoginModel');
const {jwt} = require('jsonwebtoken');
const bcrypt = require('bcrypt');


const MomLogin = async (req, res) => {

    const { email, password } = req.body;

    try {

        
    } catch (error) {
        
    }

}